---
name: daily-market-check
description: 每日 BTC/ETH 看盘与决策 SOP（v2）。一条命令跑 scripts/daily_check.py，用固定指标（价/EMA20/EMA50 + ETF 资金流刹车 + 币本位资金费率）输出「今天做 / 不做什么」五行结论。凡用户提到看盘、今天怎么操作、每日复盘、市场情况、daily market check、盯盘、今天能不能进、要不要清仓、合约能不能开，都必须用本 skill，不要自己手算或另找指标。
---

# 每日看盘决策 SOP（v2，2026-09-22 定版）

目的：用**固定不变**的一小组指标，把每天的市场信息压缩成一个可执行结论。
核心原则：**减少信息，不增加信息。** 所有计算和判断在脚本里完成，你的工作只是**运行 + 照抄**。ETF 只当**刹车**，不当油门。

## 步骤（有 bash 时，唯一路径）

1. 运行（按用户仓位选一条）：
   - 空仓：`python3 <本 skill 目录>/scripts/daily_check.py`
   - 有多仓：`python3 .../daily_check.py --position long`
   - 用户给了风险预算：加 `--risk 2`（默认 1，含义：单笔最多亏账户的 1%）
   - 用户没说仓位 → 按空仓跑，末尾加一句「有仓请告知，重跑」
2. **原样输出**脚本打印的 5 行 + 「日志」行。允许的改动只有：给「理由」润色措辞。不许增删指标、不许改数字、不许加评论。
3. 脚本自己处理数据缺失：K 线 Crypto.com → OKX → Bybit；ETF Farside → SoSoValue → Farside 备用表；费率 OKX → Bybit。
   ETF 缺失时脚本写「ETF 数据缺失｜刹车：无」，**动作与关键价照常给出**；费率缺失只影响「合约」那一段。你不要因为看到「缺失」就把动作改成「无操作」——这是最常见的跑偏，出现即错。
4. 想看原始数值加 `--json`。不需要再调 MCP 或 curl 网页。

## 无 bash 时（降级路径，尽量避免）

用 Crypto.com MCP `get_candlestick(instrument_name, timeframe="1D")` 取 BTC_USDT / ETH_USDT 日线，手算 EMA20、EMA50，按下面「规则」判状态与关键价。ETF 行写「ETF 数据缺失（无 bash）｜刹车：无」，合约段写「费率缺失→不开新单」，**趋势层照常输出动作与关键价**。

## 规则（脚本已内置，供核对；不得修改参数）

v2 与 v1 的三处改动，均来自 2013–2026 BTC 日线回测：① 不再等回踩 EMA20（等回踩使年化 57%→41%）；② 取消 2×ATR 追踪止损（它把盈亏比从 8.3 砍到 2.3）；③ 风险由仓位控制，不由止损控制。

- **状态**（三选一）：顺势 = 价>EMA20>EMA50；逆势 = 价<EMA20<EMA50；其余 = 震荡
- **距失效** = (收盘 − EMA50) / 收盘。这是单笔风险%，决定仓位
- **入场**：空仓·顺势 → 入场 = 收盘价，**不等回踩**；仓位% = 风险预算% ÷ 距失效%（例：1% ÷ 13.4% = 7.5%）
- **出场**：**唯一一条**：收盘 < EMA50 → 全部清仓。无追踪止损、无止盈
- **ETF 刹车**（命中即标，只否决新入场/新开合约）：
  20 日流入 ≤ 10/20 → 熊市节奏；本周 > 25 亿且 BTC 距 50 日高 < 10% → 资金过热；连续流入 ≥ 5 周 → 波段末端；连流 ≥ 5 天流入且 20 日 ≥ 13/20 → 牛市节奏（仅解除否决）。流出永不作空信号
- **合约层（1 倍币本位）**：顺势 且 无刹车 且 资金费率年化 < 20% → 可持 1x；任一不满足 → 不开 / 平掉合约（底仓不动）。永不 2x
- **动作**：空仓有刹车 → 「无操作（ETF 刹车：X）」；否则「可入场（风险→仓位）」或「无操作」。有仓只答「持有 / 已失效→清仓」

## 输出格式（脚本即此格式，照抄）

- **状态**：BTC 顺势（价/EMA20/EMA50，距失效 x%）｜ETH …
- **ETF**：连流 _天流入｜20日 _/20｜本周 _亿｜连续流入 _周｜刹车：无 / 熊市节奏 / 资金过热 / 波段末端（数据截至 _月_日，来源）
- **动作**：BTC …；ETH …｜合约 可持1x / 不开（原因）
- **关键价**：BTC 入场 / 失效｜ETH 入场 / 失效
- **理由**：1 句
- 日志：`日期 | BTC状态 | ETH状态 | ETF连流 | 20日占比 | 刹车 | 动作 | 合约 | 失效价`

## 硬规则

- 只输出脚本给的内容；不加 RSI/MACD/斐波那契/新闻/他人观点。
- ETF 只能**否决**动作，不能**发起**动作；缺失只影响 ETF 那一行。
- 未触发条件就写「无操作」，**不为交易找理由**；不预测方向、不给目标价、不给止盈位。
- 数据缺失以脚本判定为准，**不估算、不用记忆里的旧价**。
- 失效价一旦收盘跌破，清仓再讨论，不改参数。
- 全文中文，五行合计 ≤ 300 字符，不写导语和结语。

## 反面清单（出现即视为跑偏）

- 「动作」或「关键价」行出现「缺失」二字（趋势层缺失时脚本会写「K线缺失」，那是唯一例外）
- 以「ETF 数据缺失」或「费率缺失」为理由把趋势层改成「无操作」
- 不跑脚本，自行 curl 网页 / 调 MCP 手算，或用记忆里的旧数据
- 出现「等回踩」「止损 xxx」「止盈」「拉伸 ×ATR」——这些是 v1 已废弃的规则
- 用「可能」「或许」堆两种相反剧本
- 把「ETF 连流 N 天」或「单日大额流入」写成入场理由；把流出写成做空理由
- 建议 2x 以上合约，或用合约替代现货底仓
- 在五行之外加解释、建议、免责声明

## 规则依据（仅供回查，不写入输出）

- 趋势层：Bitstamp BTC/USD 2013-01→2026-09 日线，单边成本 0.15%。顺势进/破 EMA50 出：年化 91%、Sharpe 1.48、最大回撤 −72%（死拿 90% / 1.23 / −85%）；EMA10/30～50/200 六组 Sharpe 1.33–1.58 全高于死拿。近 6 年最大回撤 −37%/−31%，死拿 −77%/−53%
- 加 2×ATR 追踪止损：Sharpe 1.12、盈亏比 2.3；等回踩：年化再降 16 个点
- 1 倍币本位叠加（含费率 0.03%/天）：2018→今 币 1.46×，2021→今 1.26×；2x/3x 不加币只加回撤；费率 0.06%/天时转为亏币
- ETF：2024/1–2026/9 Farside。牛市段流入日占比 66%、次日延续 80%；顶后 47%、59%。单周 >25 亿且近前高：10 次 9 次后 4 周下跌。刹车叠加：Sharpe 0.61→0.68（16 笔，弱证据）
